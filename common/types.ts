export type Number = {
  number: string
  open: boolean
  reach: boolean
  bingo: boolean
  center: boolean
}

export type Room = {
  id: string
  name: string
  number: string
  history: string[]
  gifts: string[]
}
